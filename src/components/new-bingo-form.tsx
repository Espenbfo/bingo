import clsx from "clsx";
import "./bingo-square.css"

type Props = {};

export const NewBingoForm = ({}: Props) => {
    return (
        <form className={clsx("square")}>
            Test
        </form>
    );
}